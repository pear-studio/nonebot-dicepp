---
name: review-local-changes
description: Review local git changes for implementation quality, design balance (no over-engineering, no short-sightedness), and long-term impact. Launches parallel subagents for objective results.
---

# 本地改动 Review

对当前未提交的 git 改动进行客观评审：实现质量、设计平衡（避免过度设计与短视决策）、长远影响。

---

## 步骤

### 1. 获取改动内容

```powershell
git diff HEAD          # 全部本地改动（含已暂存 + 未暂存）
git diff --stat HEAD   # 改动文件概览
```

如果没有改动，输出"暂无本地修改"并退出。

### 2. 启动两个并行子 Agent

启动两个子 Agent，**同时启动，不等待其中一个完成再启动另一个**。
让他们自己去看修改，不需要把修改直接传递过去。
如果启动失败，不要一个人继续，而是向用户确认情况。

**Agent A — 实现质量**，聚焦：
- 正确性：逻辑是否有 bug，边界条件是否处理
- 健壮性：错误处理、空值、异常分支
- 测试：改动是否有对应测试，或现有测试是否需要更新
- 代码风格：是否符合项目已有规范（命名、注释、结构）

**Agent B — 设计质量**，聚焦：
- 过度设计识别：不必要的抽象层、提前泛化、"以防万一"的冗余代码
- 短视识别：硬编码常量、缺少迁移路径的 breaking change、未来明显会痛的耦合
- 一致性：是否与项目已有模式保持一致，还是引入了孤立的新范式
- 长远可维护性：改动是否在清偿技术债，还是在累积新的技术债

每个 Agent 的 prompt 应包含：
1. 完整 diff 内容
2. 明确的聚焦方向（见上）
3. 输出要求：列出发现的问题（按严重程度：严重/警告/建议），无问题时也要明确说"此方面未发现问题"

### 3. 等待两个 Agent 返回结果

收集两个 Agent 的完整输出，但不要盲信，需要再次评估和确认正确性
等待期间可使用 DicePP Shell 等工具验证改动实际效果，将验证结果纳入最终评估

### 4. 综合输出报告

将报告写入项目根目录的 .temp/ 中的.md文件, 文件名格式为 review-YYMMDD-HHMM-主题

格式如下：

```
## 本地改动 Review

**改动范围**: X 个文件，+Y 行，-Z 行

---

### 实现质量
[Agent A 结论，按严重/警告/建议分级列出，无问题时写"未发现问题"]

---

### 设计质量
[Agent B 结论，按严重/警告/建议分级列出，无问题时写"未发现问题"]

---

### 综合评估
[1-3 句话总结：改动整体质量如何，最需要关注的点是什么，是否建议直接提交]
```

---

## 注意事项

- 需要逐行阅读所有修改, 不可望文生义, 不可草率下结论
- 不确定的直接进一步看源码分析，不要在汇报中出现"若有时间可进一步确认" "若项目已有" 这样的话
- 保持客观：不因改动量小就降低标准，也不因改动量大就放宽尺度
- 严重问题（正确性 bug、数据安全）必须明确标出，不可淡化
- 对"过度设计"与"不够健壮"的判断要结合项目当前阶段，不套用通用最佳实践教条, 但新的设计或重构设计时应当参考业界通用做法
- 只报告，不自动修改代码
