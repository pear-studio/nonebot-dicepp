# DicePP 使用说明（新版可视化后台）

## 给最终用户

只需做一件事：**双击「启动骰娘.bat」**。

首次运行会自动下载 Python 和依赖（约 200MB，几分钟），完成后浏览器会自动打开 `http://127.0.0.1:2333/admin`。

之后再启动只需几秒。

---

## 详细使用流程

### 第一次使用

1. 解压本压缩包到任意目录（路径不要含中文/空格更稳）
2. 双击「**启动骰娘.bat**」
3. 等待自动下载（首次约 3-10 分钟，看网速）
4. 浏览器自动打开后台 → 设置管理员密码（至少 6 位）

### 创建第一个骰娘

1. 在后台左侧点「**+ 新建**」
2. 填实例名（例：跑团骰娘）和 QQ 号
3. 点「**LLOneBot**」按钮：
   - 如果没装 LLOneBot：跟随提示去 GitHub 下载 + 装 QQNT
   - 如果装了：把 `llbot.exe` 路径填上保存
4. 点实例上方的「**LLOneBot**」启动协议端，扫码登录 QQ
5. 在 LLOneBot 设置里把反向 WS 改成后台显示的地址（如 `ws://127.0.0.1:8080/onebot/v11/ws`）
6. 在后台点「**▶ 启动**」启动 DicePP 实例
7. 在 QQ 群里输入 `.help` 测试

### 多个骰娘

每个骰娘是独立进程 + 独立数据库 + 独立端口，互不影响。只需重复「新建实例 + 启动 LLOneBot + 启动 DicePP」即可。

### 数据管理

选中实例后切换顶部 tab：
- **概览** — 状态/端口/实时运行日志
- **跑团日志** — 检索 + 导出 (txt/md) + 增删
- **群聊查询** — 检索 chat_record 表
- **群配置** — 群配置/启停/欢迎语 CRUD
- **角色卡** — DND/COC 角色卡 CRUD
- **牌堆** — 牌堆文件 CRUD
- **随机生成器** — 随机内容文件 CRUD
- **SQLite** — 直接打开实例数据库做查询/编辑

### 更新

双击「**更新.bat**」会 `git pull` + 重新 `uv sync`。

### 卸载

双击「**卸载.bat**」会删除 `bin/`, `.python/`, `.venv/` 三个运行时目录。`data/` 不会动（保留你的骰娘数据），如要彻底清理手动删除即可。

---

## 项目目录说明

```
nonebot-dicepp-alpha/
├── 启动骰娘.bat                       ← 用户入口
├── 更新.bat
├── 卸载.bat
├── 使用说明.md                         ← 本文件
│
├── bot.py / standalone_bot.py         ← α 原有运行入口
├── pyproject.toml
│
├── scripts/deploy/windows/            ← 部署脚本
│   ├── bootstrap.ps1                  自动装 uv/Python/依赖
│   ├── start_admin.ps1                启动 WebUI + 开浏览器
│   └── update.ps1                     更新逻辑
│
├── src/dicepp_admin/                  ← WebUI 管理后台（新增）
│   ├── __main__.py                    入口 (python -m dicepp_admin)
│   ├── app.py                         FastAPI 路由
│   ├── auth.py                        密码 + cookie session
│   ├── instance_manager.py            多 DicePP 子进程管理
│   ├── llonebot_manager.py            LLOneBot 集成
│   ├── data_api.py                    业务数据 CRUD
│   ├── log_api.py                     日志检索
│   ├── audit.py                       操作审计
│   ├── ui.py                          返回 HTML
│   └── static/admin.html              单页 Web UI
│
├── src/plugins/DicePP/                ← α 原有插件代码（未动）
│   └── core/config/basic.py           加了 DICEPP_DATA_DIR 环境变量支持（多实例隔离用）
│
├── content/queries/DND5E2024.db       ← 从 β 移植的 DND5E 2024 规则库
│
├── docs/
│   ├── beta_merge_guide.md            ← β 业务模块手工合并指南
│   └── beta_merge_reference/          ← β 独有模块原文归档（待重写）
│
├── bin/        ← 运行时生成（uv.exe）
├── .python/    ← 运行时生成（独立 Python 3.10）
├── .venv/      ← 运行时生成（依赖虚拟环境）
└── data/       ← 运行时生成
    ├── admin/                         ← 后台自身的数据
    │   ├── auth.json                  密码 hash
    │   ├── sessions.json              cookie 会话
    │   ├── instances.json             实例清单
    │   └── audit.db                   操作审计
    ├── instances/<instance_id>/       ← 每个骰娘的独立数据
    │   ├── bots/<qq_id>/bot_data.db
    │   ├── bots/<qq_id>/log.db
    │   ├── logs/runtime.log
    │   └── config/
    └── llonebot/
        ├── config.json                LLOneBot 路径配置
        └── <instance_id>.log
```

---

## 常见问题

### 启动 bat 卡在某一步？
- 第一次「同步项目依赖」步骤最慢（首次 3-5 分钟），耐心等。
- 如反复失败，把 `bin/uv.exe` 删掉重跑（强制重新下载）。

### uv 下载失败？
- 自检脚本会先尝试国内 gh-proxy 镜像，失败回退官方 GitHub。
- 如果两个都不通，自己去 https://github.com/astral-sh/uv/releases 下载 `uv-x86_64-pc-windows-msvc.zip`，解压把 `uv.exe` 放到本项目的 `bin/` 目录下，再双击启动 bat。

### Python 下载慢？
- bat 已经把 `UV_PYTHON_INSTALL_MIRROR` 设成国内代理，正常都能拉到。
- 如不通，去 https://github.com/astral-sh/python-build-standalone/releases 下载 `cpython-3.10.*-x86_64-pc-windows-msvc-install_only.tar.gz`，解压到 `.python/3.10.*/` 即可。

### LLOneBot 不识别？
- 后台右上角点「LL」按钮，手动指定 `llbot.exe` 路径并保存。
- 检测的常见位置：`Program Files\LLOneBot\`、桌面 `LLONEBOT\`、本项目同级目录 `LLONEBOT\`。
- 如果你是从 β 整合包来的，把 β 整合包里的 `LLONEBOT` 文件夹复制到本项目同级目录，会自动识别。

### 多个骰娘端口冲突？
- 默认从 8080 开始分配，自动避让被占用的端口。
- 如要换段，启动前设环境变量 `DPP_INSTANCE_PORT_START=9000`。

### 后台访问端口冲突？
- 默认 2333。如冲突，设置环境变量 `DPP_ADMIN_PORT=23333` 再启动。

---

## 给开发者

### 后台不需要 admin 管理时直接跑 α
跟 α 原本一样：
```bat
scripts\dev\install.bat
scripts\dev\run.bat
```

### 后台开发调试
```bat
.venv\Scripts\python.exe -m dicepp_admin
```
默认监听 `127.0.0.1:2333`。改 host/port：
```bat
set DPP_ADMIN_HOST=0.0.0.0
set DPP_ADMIN_PORT=8000
.venv\Scripts\python.exe -m dicepp_admin
```

### 想合并 β 的 COC/宏/变量等业务模块？
看 `docs/beta_merge_guide.md`，里面有每个模块的重写步骤和工作量评估。
