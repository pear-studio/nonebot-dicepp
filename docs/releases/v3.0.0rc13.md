# v3.0.0rc13

- 状态: 草稿（version-release 发版时补全镜像、artifact 与风险元数据）

## Changed

- `/api/bots/status` 的 `last_heartbeat_ts` 字段 JSON 类型由 string 变为 number（epoch 秒，解析失败时为空字符串）。Dashboard 前端 `fmtRelativeTime` 经 `parseFloat` 兼容两种类型；按字符串处理该字段的外部脚本需要调整。
