# DicePP CI — triggers the shared test suite for branch and PR checks

name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

permissions:
  contents: read

jobs:
  test-suite:
    name: Test Suite
    uses: ./.github/workflows/test-suite.yml
