# v3.0.0rc1

- 镜像: ghcr.io/pear-studio/nonebot-dicepp:v3.0.0rc1
- Windows: DicePP-v3.0.0rc1-win64.zip
- 数据变更: no
- 配置变更: no

## Added
- 首个 `3.0.0` 发布候选版，用于验证 GHCR 镜像、Windows EXE 和 GitHub Prerelease 发版链路。
- Windows EXE 打包流程包含 `--version` 版本一致性检查和 `--smoke-check` 冒烟自检。

## Changed
- RC 发版只推送同名镜像 tag，不更新 `latest`。
- GitHub Release 对 RC tag 标记为 Prerelease。

## Fixed
- 暂无。

## Deprecated
- 暂无。

## Risk Notes
- 这是发版链路验证用 RC，不作为正式生产版本部署。
- 不包含数据库 schema 变更、配置格式变更或数据迁移。
